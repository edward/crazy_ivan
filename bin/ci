#!/usr/bin/env ruby -wKU

require 'lib/crazy_ivan.rb'

output_directory = ARGV[1]

Dir['*'].each do |dir|
  report = ReportAssembler.new(output_directory)
  
  if File.directory?(dir)
    report.test_results.push(TestRunner.new(dir).invoke)
  end
  
  report.generate
end